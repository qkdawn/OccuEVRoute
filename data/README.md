# Data

这个目录放代码运行需要的小型、派生或整理后的数据。

- `raw/`: 小型原始输入或从大数据中抽取的样本。
- `interim/`: 中间处理结果，可以重新生成。
- `processed/`: 模型训练和路线规划直接使用的清洗后数据。
- `external/`: OSMnx/OpenStreetMap 等外部路网导出。

大型 UrbanEV 数据当前保留在 `ML/Data/`。
