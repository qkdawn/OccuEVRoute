[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [
    "ML/Data",
    "data/processed",
    "frontend",
    "models",
]

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E402", "E501"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]
